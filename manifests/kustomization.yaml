apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: modal-system

resources:
  - rbac.yaml
  - deployment.yaml

# Note: secrets-template.yaml is provided as a template and should be customized
# for your environment. It is not included by default.
# To use it, create your own secret or uncomment and customize:
# - secrets-template.yaml

